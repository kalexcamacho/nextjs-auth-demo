specVersion: "v1"
serviceId: "phoenix"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "phoenix-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
